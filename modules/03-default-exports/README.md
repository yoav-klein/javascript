# Default exports
---

You can have a single `default` export per module.
In this example, the `mylib.js` module exports a default function.
Then, in the `main.js` you can import the default function with any name you want.

