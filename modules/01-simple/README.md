# Simple example
---

Demonstrates the followings:
* importing JS modules from HTML
* importing module features from other modules
* modules can only be imported from modules, not regular scripts
* module top-level code is executed whenever you import features from a module, and only once!